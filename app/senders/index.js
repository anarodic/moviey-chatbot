const sendMapMessage = require('./sendMapMessage');
const sendMessage = require('./sendMessage');
const sendTextMessage = require('./sendTextMessage');

module.exports = {
	sendMapMessage,
	sendMessage,
	sendTextMessage
};